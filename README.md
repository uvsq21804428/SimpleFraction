#Contenu du projet:
  un fichier Main.java qui contient le code source principal du projet
  un fichier Fraction.java qui contient le code source de la classe Fraction

#Pour compiler le programme:
  javac *.java
  
#Pour executer le programme:
  java Main
